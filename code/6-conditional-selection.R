# AND
# &
is_married <- c(TRUE, TRUE, FALSE)
is_happy <- c(TRUE, FALSE, FALSE)

is_married & is_happy

# &&
# returns a single value
# is_married && is_happy

# OR
# |

is_married | is_happy

# ||
# returns a single value
